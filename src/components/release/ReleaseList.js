import ReleaseSummary from "./ReleaseSummary";

const ReleaseList = (props) => {
  return (
    <div>
      <h1>Release List</h1>
      <ReleaseSummary />
    </div>
  );
};

export default ReleaseList;
